#include <stdio.h>

int main(void) {
    int arr[5][5], n;

    for (int i = 0; i < 5; i++) {
        printf("Enter row %d: ", i + 1);
        for (int j = 0; j < 5; j++) {
            scanf(" %d", &n);
            arr[i][j] = n;
        }
    }

    for (int i = 0; i < )

    return 0;
}
